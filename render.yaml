services:
  - type: web
    name: telegram-server
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: BOT_TOKEN
        value: <your_telegram_bot_token>
